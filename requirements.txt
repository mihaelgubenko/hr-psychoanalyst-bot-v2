# Основные зависимости для HR-Психоаналитического бота v2.0
python-telegram-bot[job-queue]>=20.0
openai>=1.0.0
python-dotenv>=1.0.0

# Точный подсчет токенов для OpenAI
tiktoken>=0.5.0

# Валидация и конфигурация
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Конфигурационные файлы
pyyaml>=6.0

# Тестирование (опционально для dev)
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# УДАЛЕНЫ НЕИСПОЛЬЗУЕМЫЕ ЗАВИСИМОСТИ:
# - textblob (не используется)
# - vaderSentiment (не используется)
# - scikit-learn (не используется)
# - numpy (не используется)
# - structlog (не используется)
# - prometheus-client (не используется)
# - requests (не используется, есть httpx в python-telegram-bot)
# - asyncio-mqtt (не используется)